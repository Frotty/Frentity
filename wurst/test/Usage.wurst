package Usage
import Buff
import Fx
import FText
import ClosureTimers

class TestUnitEntity extends UnitEntity

	construct()
		super(createUnit(players[0], 'hfoo', vec2(0,0), angle(0)))

class Testos
	real r = 0

	construct(real r)
		this.r = r
		createUnit(players[0], 'hfoo', vec2(r,r), angle(0))

init
	for i = 0 to 10
		createFText("Fuck", GetRandomReal(2,12), 4, vec3(i * 32., 0, 0), vec2(0,2))

	doPeriodically(2, () -> begin
		for i = 0 to 5
			createFText("Fuck", GetRandomReal(2,12), GetRandomReal(1,5), vec3(i * 32., 0, 0), vec2(GetRandomReal(-1,1),GetRandomReal(1,2)))
	end)